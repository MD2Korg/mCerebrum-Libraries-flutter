

class Summary{

/*
  Map<String, dynamic> _summary;
  Future<void> getSummary() async {
    _summary = await Phonesensor.getSummary();
  }
  bool isRunning(){
    if(_summary==null || !_summary.containsKey("is_running")) return false;
    return _summary["is_running"];
  }
  String getRunningTime(){
    if(_summary==null || !_summary.containsKey("running_time")) return '';
    int runningTime = _summary["running_time"];
    runningTime = (runningTime/1000).round();
    int sec = runningTime%60;
    runningTime = (runningTime/60).round();
    int min = runningTime%60;
    int hour = (runningTime/60).round();
    String res = (hour.toString().padLeft(2, '0'))+':'+(min.toString().padLeft(2, '0'))+":"+(sec.toString().padLeft(2, '0'));
    return res;
  }
*/
}